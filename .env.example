# Copy to .env and fill in your Neon connection string (use the POOLED string).
# In Netlify, set this same variable under Site settings → Environment variables.
DATABASE_URL=postgresql://user:password@ep-xxxx-pooler.region.aws.neon.tech/dbname?sslmode=require
